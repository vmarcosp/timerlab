type theme = {
  id: string,
  name: string,
  primaryColor: string,
  secondaryColor: string,
  background: string,
  default: bool,
};

type config = {
  title: string,
  description: string,
  time: string,
  theme: string,
};
